apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: kafka-manual
  selector:
    matchLabels:
      app: kafka
  resources:
    requests:
      storage: 1Gi
